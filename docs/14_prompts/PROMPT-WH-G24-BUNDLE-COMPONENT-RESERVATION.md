Implement the Warehouse `catalog.bundle.v1` component-line reservation sign-off. Keep Warehouse as stock/reservation owner for existing product lines only. Add docs/source/tests/verifier evidence that `bundleId`, synthetic bundle SKU/stock, and bundle contract evidence are rejected as reservation identity while normal component `productId` reservation still works. Do not edit other repos, migrations, deploy scripts, secrets, Kubernetes manifests, or live stock. Validate with focused Jest, static verifier, build, and `git diff --check`.
